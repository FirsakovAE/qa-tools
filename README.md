# Vue Inspector — QA Tools for Vue Applications

**Vue Inspector** — инструмент для инспекции, отладки и тестирования Vue-приложений.
Предназначен для QA-инженеров, аналитиков и поддержки, которым важно понимать **реальное состояние фронтенда**, а не работать с приложением как с чёрным ящиком.

Инструмент позволяет:

* видеть и изменять `props` Vue-компонентов
* читать и редактировать `state` и `getters` Pinia
* управлять сетевыми запросами (breakpoint + mock response)
* работать без прокси, сертификатов и пересборки проекта

---

## ❓ Зачем нужен Vue Inspector

В современных Vue-приложениях большая часть логики:

* не видна в DOM
* не всегда отражена в network-запросах
* не хранится в `localStorage` или `sessionStorage`

Основное поведение приложения определяется:

* входными `props` компонентов
* глобальным состоянием Pinia
* сетевыми запросами и их ответами

Классические инструменты (DevTools, Vue DevTools) не дают полного доступа ко всем этим уровням одновременно.

**Vue Inspector решает эту проблему**, предоставляя единый интерфейс для анализа и управления:

```
Network → Store (Pinia) → Props → Render
```

---

## 📦 Возможности

### 🔹 Props (Vue Components)

* Просмотр всех Vue-компонентов на странице (включая logic-only)
* Отображение компонентов как API интерфейса:

  * входные данные (`props`)
  * привязка к DOM
* Подсветка DOM-элементов при наведении
* Расширенный поиск:

  * по имени компонента
  * по DOM-элементу
  * по ключам и значениям props
* Избранные компоненты
* Редактирование props:

  * напрямую в JSON
  * с валидацией
  * без пересборки проекта
  * без перезагрузки страницы

---

### 🔹 Store (Pinia)

Pinia — это реальный источник истины во Vue-приложениях.

**Vue Inspector позволяет работать с Pinia напрямую:**

* Просмотр всех stores, обнаруженных на странице
* Отображение:

  * `state`
  * `getters`
* Понимание, какие stores реально участвуют в логике экрана
* Расширенный поиск:

  * по имени store
  * по ключам
  * по значениям
* Редактирование:

  * `state`
  * `getters`
  * без перезагрузки страницы
  * без изменения кода приложения

Это позволяет понять:

* почему интерфейс находится в текущем состоянии
* какие условия активировали логику
* почему один и тот же экран ведёт себя по-разному

---

### 🔹 Network

Вкладка Network предоставляет контроль над сетевыми запросами **без использования прокси**.

Поддерживается:

* просмотр HTTP-запросов
* установка breakpoint на request
* изменение запроса до отправки
* полный mock response

#### Breakpoint Request

* остановка запроса до отправки на сервер
* редактирование:

  * заголовков
  * query-параметров
  * тела запроса

#### Mock Response

* подмена ответа сервера
* настройка:

  * HTTP-статуса
  * заголовков
  * тела ответа
  * задержки

Особенности:

* работает только в текущей вкладке
* не влияет на другие сайты
* не требует сертификатов
* не ломает окружение браузера

---

## 🚀 Установка и запуск

Инструмент поддерживает **два режима работы**:

* браузерное расширение (рекомендуется)
* standalone-режим (для корпоративных окружений)

---

### 🧩 Установка как расширение браузера

1. Соберите проект:

```bash
npm run build
```

2. Откройте:

```
chrome://extensions/
```

3. Включите **Developer mode**

4. Нажмите **Load unpacked**

5. Выберите папку `dist/`

После этого расширение готово к использованию.

#### Инкогнито-режим

1. Откройте настройки расширения
2. Включите **Allow in Incognito**

---

### 📎 Standalone-режим (без установки расширения)

Используется в окружениях, где установка расширений запрещена.

1. Перейдите на страницу:

```
https://firsakovae.github.io/qa-tools/
```

2. Перетащите кнопку **📦 Vue Inspector** в панель закладок

3. Откройте страницу с Vue-приложением

4. Кликните по закладке

Инспектор загрузится прямо на странице.

⚠️ При обновлении страницы инструмент выгружается и требует повторного запуска.
Если установка расширений разрешена — рекомендуется использовать расширение.